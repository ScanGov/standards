---
layout: layouts/default.html
title: "Guidance"
date: 2024-03-08
author: ScanGov
description: "ScanGov Standards are based on these established web protocol, guidelines and best practices."
topics:
  - Guidance
---

<div class="table-responsive">
    <table class="table table-bordered table-hover shadow">
      <thead>
        <tr>
          <th scope="col" class="p-3">Topics</th>
          <th scope="col" class="p-3">Guidance</th>
          <th scope="col" class="p-3">Description</th>
          <th scope="col" class="p-3">Standards</th>
        </tr>
      </thead>
      <tbody>
        {% for key, item  in guidance %}
        <tr>
          <td class="p-3">
            <div class="d-flex flex-wrap">
              {% for topic in item.topics %}
                <a href="{{ audits[topic].url }}" class="icon-link d-inline-flex align-items-center mb-1 me-1 text-decoration-none">
                  <i class="fa-solid fa-{{ audits[topic].icon }} me-2" title="{{ audits[topic].displayName }}"></i>
                </a>
              {% endfor %}
            </div>
          </td>
          <td class="p-3">
            <div>
              <a href="{{ item.docs }}">{{ item.displayName }}</a>
            </div>
          </td>
          <td class="p-3">
            <div>
              {{ item.description }}
            </div>
          </td>
          <td class="p-3">
            <ul>
              {% for standard in item.standards %}
                <li><a href="{{ standard.url }}">{{ standard.displayName }}</a></li>
              {% endfor %}
            </ul>
          </td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>