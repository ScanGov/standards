---
layout: layouts/default.html
title: "Government digital experience standards"
date: 2025-02-01
modified: 2025-03-17
author: ScanGov
description: "Based on public policy, web protocol, guidelines and best practices."
topics:
  - Standards
---

  <div class="table-responsive">
    <table class="table table-bordered table-hover shadow">
      <thead>
        <tr>
          <th scope="col" class="p-3">Indicators <a href="https://docs.scangov.org/indicators" style="color: inherit;"><i class="fa-solid fa-circle-question" aria-label="Indicators"></i></a></th>
          <th scope="col" class="p-3">Standard</th>
          <th scope="col" class="p-3">Why</th>
          <th scope="col" class="p-3">Guidance <a href="https://standards.scangov.org/guidance" style="color: inherit;"><i class="fa-solid fa-circle-question" aria-label="Guidance"></i></a></th>
        </tr>
      </thead>
      <tbody>
        {% for key, value in audits %}
          {% for item in audits[key].attributes %}
          <tr>
            <td class="p-3 col-1">
              <div class="d-flex flex-wrap">
                <a href="{{ value.url }}" class="icon-link d-inline-flex align-items-start mb-1 me-1 text-decoration-none">
                  <i class="fa-solid fa-{{ value.icon }} me-2" title="{{ topic }}" aria-label="{{ value.displayName }}"></i>
                </a>
              </div>
            </td>
            <td class="p-3 col-4">
                <div>
                  <a href="{{ item.docs }}">{{ item.displayName }}</a>
                </div>
                <div>
                  <span class="small">{{ item.description }}</span>
                </div>
            </td>
            <td class="p-3 col-4">
                <div>
                  <span class="small">{{ item.why }}</span>
                </div>
            </td>
            <td class="p-3 col-3">
            <ul class="small">
              {% for guidance in item.guidance %}
                <li><a href="{{ guidance.url }}">{{ guidance.displayName }}</a></li>
              {% endfor %}
            </ul>
            </td>
          </tr>
          {% endfor %}
        {% endfor %}
      </tbody>
    </table>
  </div>
